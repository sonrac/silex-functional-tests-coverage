build:
  environment:
        php:
            version: 7.1
  dependencies:
          before:
              - composer require doctrine/migrations --prefer-dist --no-interaction
              - composer require silex/silex --prefer-dist --no-interaction
              - composer require symfony/browser-kit:^3.4 --prefer-dist --no-interaction
  tests:
    override:
      -
        command: './vendor/bin/phpunit --coverage-clover coverage.xml'
        coverage:
          file: 'coverage.xml'
          format: clover
imports:
    - php
checks:
  php:
    code_rating: true
    duplication: true
filter:
  excluded_paths:
    - vendor/*
    - tests/*
tools:
    php_sim: false
    php_cpd: false
    php_pdepend: true
    php_analyzer: true
    php_changetracking: true
filter:
    excluded_paths:
        - tests/*
        - vendor/*
